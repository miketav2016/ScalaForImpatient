package ch17Futures.exercises.ex7

object CountsThePrime extends App {
  def countP(n: Int, p: Int) ={

  }

  val n = 100
  val p = Runtime.getRuntime.availableProcessors()
  val res = countP(n, p)
  print(res)
}
